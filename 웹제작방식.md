# 오성 웹 프로젝트 제작 방식

## 기본 스택 (모든 프로젝트 통일)
| 항목 | 선택 | 비고 |
|------|------|------|
| Framework | Next.js 14+ (App Router) | create-next-app |
| Style | Tailwind CSS | v4 |
| DB | Supabase | oseong's Project 통합 사용 |
| Deploy | Vercel | GitHub 연동 |
| Language | TypeScript | 필수 |
| 테마 | 다크 (zinc-900 배경) | 전 프로젝트 통일 |

## 프로젝트 위치
- 로컬: `C:\dev\{프로젝트명}`
- GitHub: `oseong27436/{프로젝트명}`
- Vercel: 자동 배포 (GitHub 연동)

## Supabase 구조
- **통합 프로젝트**: `oseong's Project` (ID: `ovbkdgjrkstzyahidyzv`)
  - URL: `https://ovbkdgjrkstzyahidyzv.supabase.co`
  - 모든 앱의 테이블을 한 프로젝트에 통합 관리
- **테이블 목록**:
  | 앱 | 테이블 |
  |----|--------|
  | fridge | fridge_contents, food_items |
  | inbody | inbody_records |

## 환경변수 관리
- **원본(Dropbox)**: `C:\Users\GKL\Dropbox\01코딩관련\claude-setup\{프로젝트명}.env`
- **로컬 사용**: `C:\dev\{프로젝트명}\.env.local` (Git 제외)
- **Vercel**: CLI 또는 API로 프로젝트 env 등록

## Supabase RLS (Row Level Security) 주의사항

`apply_migration`으로 테이블 생성 시 **RLS가 자동으로 켜짐**.
RLS가 켜진 상태에서 policy가 없으면 anon/authenticated 유저는 데이터를 읽지 못함 (빈 배열 반환).

테이블 생성 후 반드시 실행:
```sql
-- RLS 끄기 (공개 읽기 허용 시)
ALTER TABLE {테이블명} DISABLE ROW LEVEL SECURITY;

-- 또는 anon 권한 명시적 부여
GRANT SELECT, INSERT, UPDATE ON {테이블명} TO anon, authenticated;
```

확인 방법:
```sql
SELECT relname, relrowsecurity FROM pg_class WHERE relname = '{테이블명}';
-- relrowsecurity = true면 RLS 켜진 상태
```

## 신규 프로젝트 생성 순서
1. `cd C:\dev && npx create-next-app@latest {프로젝트명}`
2. Supabase `oseong's Project`에 테이블 추가 (`apply_migration` 사용)
3. **RLS 비활성화**: `ALTER TABLE {테이블명} DISABLE ROW LEVEL SECURITY;`
4. `.env.local` 생성 (Supabase URL + anon key)
5. Dropbox에 `{프로젝트명}.env` 백업
6. GitHub 레포 생성 후 push
7. `npx vercel --prod --yes --token {토큰}` 배포
8. Vercel에 env 등록 (Vercel API POST `/v10/projects/{id}/env`)

## Vercel 토큰
- 위치: `C:\Users\GKL\Dropbox\01코딩관련\claude-setup\.vercel.env`
- 만료 없는 토큰 (vcp_ 형태)

## 프로젝트 목록
| 앱 | URL | 설명 |
|----|-----|------|
| fridge | (Vercel URL) | 냉장고 재고 관리 |
| inbody | https://inbody-tau.vercel.app | 인바디 주차별 기록 & 그래프 |
| stocks | https://stocks-pearl-one.vercel.app | 주식 포트폴리오 & 그룹 수익 비교 |
| portal | 미정 | fridge + inbody 통합 포털 |

## UI 컨벤션
- 배경: `bg-zinc-900`
- 카드: `bg-zinc-800` + `rounded-xl`
- 텍스트: `text-zinc-100` / 서브: `text-zinc-400`
- 차트: Recharts (꺾은선 기본)
- 아이콘: 이모지 사용 (라이브러리 없이)
